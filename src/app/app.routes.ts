import { Routes } from '@angular/router';
import { LandingRedirect } from './components/landing-redirect/landing-redirect';

export const routes: Routes = [
    { path: '', component: LandingRedirect },
];
